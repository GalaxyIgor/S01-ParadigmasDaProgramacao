fn main() {
    // Loop `for` que itera sobre o intervalo de 3 a 100 (inclusive)
    for n in 3..101 {
        print!("{} ", n); // Imprime o valor de `n` seguido de um espaço
    }

    println!(); // Imprime uma nova linha após o loop para melhorar a formatação
}